<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evaldo.terminalperquisacliente">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".telasPerguntas.TelaFinalAgradecimentoActivity"></activity>
        <activity android:name=".activity.PrincipalActivity" />
        <activity android:name=".telasPerguntas.PerguntaTelefoneOuEmailActivity" />
        <activity android:name=".telasPerguntas.PerguntaAbertaActivity" />
        <activity android:name=".telasPerguntas.PerguntaSimOuNaoActivity" />
        <activity android:name=".telasPerguntas.PerguntaEmotion6Activity" />
        <activity android:name=".telasPerguntas.PerguntaEmotion3DivulgacaoActivity" />
        <activity android:name=".activity.TelaGerenciadorActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>